# Spark 的全部运行依赖（summit2md + notes2insight + core 共用这一份）。
# 版本固定成当前验证过的组合；升级时改这里、跑一遍测试再提交。
flask==3.1.3
werkzeug==3.1.8        # spark.py 直接 import 它的 DispatcherMiddleware / run_simple
anthropic==0.122.0
certifi==2026.7.22
yt-dlp==2026.7.4
beautifulsoup4==4.15.0
lxml==6.1.1
feedparser==6.0.14
pypdf==6.18.1
python-docx==1.2.0
python-pptx==1.0.2
